/**
 * This service handles all annotator handlers for codr.
 */

import App from "./app";

class Annotator {
  private app: App;
  constructor(app: App) {
    this.app = app;
  }
}

export default Annotator;
