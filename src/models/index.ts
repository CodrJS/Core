import { default as User } from "./User";
import { default as UserAbility } from "./User.ability";
import { default as Profile } from "./User";
import { default as ProfileAbility } from "./Profile.ability";

export const Model = {
  User,
  Profile,
};
export const Ability = {
  UserAbility,
  ProfileAbility,
};
